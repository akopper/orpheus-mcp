{
  "mcpServers": {
    "orpheus-tts": {
      "command": "./run_mcp_server.sh",
      "args": [],
      "env": {
        "ORPHEUS_MODEL_PATH": "${ORPHEUS_MODEL_PATH}",
        "ORPHEUS_API_URL": "${ORPHEUS_API_URL}",
        "MCP_TRANSPORT": "stdio"
      },
      "description": "Orpheus TTS MCP Server - Generate speech and audiobooks with 25 voices across 8 languages. Requires llama-server running separately.",
      "lifecycle": "on-demand",
      "tools": [
        {
          "name": "generate_speech",
          "description": "Convert text to speech using Orpheus TTS. Returns path to generated WAV file."
        },
        {
          "name": "list_voices",
          "description": "List all available voices for speech generation."
        },
        {
          "name": "get_voice_info",
          "description": "Get detailed information about a specific voice including language and supported features."
        },
        {
          "name": "estimate_tokens",
          "description": "Estimate token count for text to help plan chunking strategy for long texts."
        }
      ]
    }
  }
}
